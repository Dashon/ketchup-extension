{
  "name": "ketchup-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Ketchup Capture — Chrome Extension for manual DOM recording via rrweb",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "echo 'Load the extension as unpacked in chrome://extensions'"
  },
  "dependencies": {
    "rrweb": "^2.0.0-alpha.17"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
